<!DOCTYPE html>
<html lang="en">

{% include "head.liquid" %}

<body class='{{ class }} line-numbers {% if tags %}tags-{{tags | join: "-"}}{% endif %}{% if teacher%}notes{%endif%}' style="{% if background %}background: {{background}} {%endif%}">
    <header class="site__header">
        <h1 class="site__title">
            <a href='/'>Max Kohler</a>
        </h1>
        <p class='site__tagline'>"Graphic Designer's Designer"</p>
    </header>
    {{ content }}

    <script src="{{ '/js/main.js' | url }}"></script>
</body>

</html>