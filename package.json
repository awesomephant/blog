{
  "name": "blog",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:sass": "sass ./css/main.scss ./_includes/style.css --no-source-map --style=compressed",
    "build:eleventy": "eleventy",
    "watch:sass": "sass ./css/main.scss ./_includes/style.css --watch --no-source-map --style=compressed",
    "watch:eleventy": "npx @11ty/eleventy --serve --quiet --incremental",
    "start": "run-p watch:*",
    "build": "run-s build:*"
  },
  "author": "Max Kohler",
  "license": "ISC",
  "homepage": "/",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "@11ty/eleventy-plugin-inclusive-language": "^1.0.0",
    "@11ty/eleventy-plugin-rss": "^1.0.7"
  },
  "dependencies": {
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.0.1",
    "sass": "^1.29.0",
    "jsdom": "^16.4.0",
    "npm-run-all": "^4.1.5"
  }
}